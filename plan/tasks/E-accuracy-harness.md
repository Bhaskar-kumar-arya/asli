# E — Accuracy harness and test set
**Priority:** P1 (start collecting photos on Thursday) · **Wave 1** · **Sessions:** 1 · **Depends on:** T02

## Read first
CLAUDE.md, docs/TESTING.md (Test set, Accuracy metrics), docs/PRIVACY.md (Demo data), docs/MATCHING.md

## Goal
Measured, honest accuracy numbers for the dashboard, writeup and video.

## Owns
`tools/accuracy/**`, `testset/**`

## Deliverables
1. `testset/README.md`: photo collection guide for the human (what to shoot, variety of foil/light/angle, redaction, naming, labelling).
2. Label helper CLI `tools/accuracy/label.ts`: shows image path, asks for truth fields, writes `<id>.json`.
3. Runner `tools/accuracy/run.ts --stage int [--method strip|bill]`: uploads each image through the real API (test user), records extracted fields, latency and tokens; computes metrics in docs/TESTING.md; seeded tier test (seeds a matching fixture row per item in a dedicated test-only table prefix or uses `/v1/checks` with a known seeded row).
4. Report: JSON + a markdown summary table; uploads `latest.json` to the public bucket.
5. Comparison mode: run twice (e.g. prompt v1 vs v2, or strip vs bill vs QR) and print the difference — for the learning log.

## Acceptance criteria
- [ ] ≥ 30 strips and ≥ 10 bills labelled (human collects; the session prompts and tracks progress)
- [ ] One full run on `int` produces a report
- [ ] At least one measured comparison written to the learning log

## Out of scope
Dashboard UI (J).

---
## Handoff (the session updates this before stopping)
**Status:** IN PROGRESS
**Stage deployed:** `int` now has a live scan pipeline (`LaneCStack-int` redeployed 2026-09-19 with C's Gemini-default extraction - see plan/INTEGRATION_LOG.md). Verified for real with a manual smoke test: a real strip photo through `/v1/uploads`+`/v1/scans` returned a correct `NO_ALERT_FOUND`, and the mock-flagged strip below returned a correct `FLAGGED` with a real CDSCO source citation. **The harness's own `run:accuracy` has now also been run for real against `int`** (see next section) - both acceptance criteria 2 and 3 are met.
**Real `run:accuracy` run (this session, 2026-09-19):** `ASLI_TEST_USER_EMAIL=asha.demo@asli.internal ASLI_TEST_USER_PASSWORD=<seed-demo's DEMO_PASSWORD> pnpm --filter @asli/accuracy-harness run:accuracy -- --stage int --upload` ran end-to-end for the first time and hit a real bug: `resolveStageConfig` in `tools/accuracy/src/aws.ts` built shared-resource SSM paths under `/asli/<--stage>/...` instead of `/asli/<SHARED_STAGE>/...`, so it always failed against a real lane stage (only `T02`'s `shared-stack.ts` publishes `api/endpoint`/`cognito/userPoolClientId`/`bucket/public`, only under `dev-shared`). Fixed in `tools/accuracy/src/aws.ts` + `run.ts` (now reads `SHARED_STAGE` env, default `dev-shared`, decoupled from the report's own `--stage`) - full writeup in `submission/LEARNING_LOG.md`'s 2026-09-19 E entry. After the fix, a real run completed: 15 strips + 1 bill scored, batch-exact 73.3%, manufacturer STRONG 86.7%, expiry-month exact 40.0%, seeded tier-correctness 44/44 (100%), avg latency 3574ms. Report uploaded to `s3://asli-dev-shared-public/metrics/accuracy/latest.json` for J's dashboard; local copy at `tools/accuracy/out/2026-09-19T11-15-31-140Z.{json,md}` (gitignored, not committed - that's by design, same as every other run).
**Testset counts (this session):** 15/30 strips, 6/10 bills labelled and Zod-validated (was 0/30, 0/10 at session start; 14/30 strips + 1/10 bills after the first sourcing pass; bills went to 6/10 in a later follow-up in this same session). See `testset/sources.md` for full per-photo attribution and an honest account of the shortfall.
- **bills 002-006** (new, follow-up in this session): not Commons-sourced - two team members provided their own real Tata 1mg pharmacy order PDFs (5 invoices), exactly matching `docs/PRIVACY.md`'s own intended path for closing this gap ("team members' own medicines; bills redacted before adding to `testset/`"). Each PDF page was rasterized (PyMuPDF, 3x zoom) and redacted (patient name/address, doctor name/address, both order QR codes, invoice/order ID block all blacked out, found from each page's real text layout since address length and table row count vary per invoice and shift the compliance QR's position) before being added to the repo - see `testset/sources.md`'s "bills 002-006" note for the exact method and what was checked. Adds 9 real Indian-market medicine line items with legible batch/manufacturer/expiry (Zerodol P, Betadine Gargle Mint, Azimax 500, Monticope, Pantakind, Azithral 500, Norflox-TZ RF, Electral Powder, Pezover-DSR). Original unredacted PDFs were never committed and were deleted after the redacted images were verified clean by eye (caught and fixed two real redaction gaps in early attempts - a doctor address wrapping below the box, and the compliance QR's position varying with the item table's row count - both fixed by computing box coordinates from each page's actual PDF text layout instead of a fixed pixel guess).
- **strip-015-mock-flagged-e9aiy029** (new, added after the int deploy above): not a sourced photo - a disclosed mock/demo strip, built by compositing a printed label panel (the real CDSCO NSQ row from `fixtures/demo/replay-1.json`: Feb-2026, batch E9AIY029, Pharma Force Lab) under strip-003's real CC BY-SA 4.0 blister photo, with a red "MOCK / DEMO STRIP - NOT A REAL PRODUCT" banner, matching the disclosed-mock-strip convention already established in `submission/DEMO_SCRIPT.md`/`docs/PRIVACY.md`. This exists because a real photo of that literal real-world flagged batch is essentially unfindable (it's one specific Indian generic manufacturing run, not a product with public photos) - a request from the human to specifically exercise/demo the FLAGGED path end-to-end. Live-verified: Gemini read every field at 0.95 confidence, matching returned `tier: FLAGGED`, `reasonCodes: [BATCH_EXACT, MFR_STRONG]`, with the real CDSCO source URL and `demo: true`. Useful for both this harness's tier-correctness coverage and the submission video/demo.
- Sourcing method: since no human was available to shoot real photos this week, sourced real, CC-licensed medicine-strip and pharmacy-bill photos from Wikimedia Commons (its `list=search`, `list=categorymembers`, and per-uploader `list=allimages` API), inspecting ~90 candidate blister/strip photos and ~10 candidate bill/receipt photos by eye (zoomed crops via a local PIL script, not committed) before writing any ground truth - no field was guessed. Every image is real (not synthetic/AI-generated) and unaltered except downscaling and, for the one bill, cropping to redact anything that wasn't the medicine line/price.
- Strips: 14/30, all with a confidently legible `batchNumber` (mandatory field - candidates where it wasn't clearly readable were dropped rather than guessed). Only 2 of the 14 are genuinely Indian-market packs (Neorelax MR / Meyer Organics via Acme Generics, and Fincover / Micro Labs) - Indian-brand searches (Cipla, Sun Pharma, Lupin, Alkem, Mankind, Torrent, Zydus Cadila, Dr Reddy's) mostly returned EU parallel-import regulatory PDFs, not photos. The other 12 are real EU/Philippines-market packs (KRKA, Zentiva, Novartis, Bayer, AstraZeneca, Takeda, GSK, Organon) with legible batch/lot/exp - still useful for testing the vision pipeline's field-reading accuracy, just not India-specific.
- Bills: 6/10 (up from 1/10 Commons-only). Real, itemised, CC-licensed Indian pharmacy bill photos are essentially unavailable under an open license (searched Commons full-text and Openverse for "pharmacy bill/receipt/cash memo/invoice" - hits were 19th-century scanned journals or a single blank receipt *template* graphic, never a real filled Indian bill). The one Commons find is a real Dutch pharmacy till receipt (Apotheek Hillegersberg, Rotterdam) with one compounded-medicine line and price - no batch/manufacturer/expiry (normal for an EU till receipt) and not Indian-format, kept anyway since it's real, licensed, and PII-clean. Two similar Dutch receipts found in the same search were card-payment slips with no medicine line and were not used. The remaining 5 bills are real team-provided Indian pharmacy invoices, redacted - see the "bills 002-006" note above.
- Verification: ran `loadTestset()` from `tools/accuracy/src/testset.ts` (unmodified) against the real `testset/` directory from a scratch script - all 15 items parse against `LabelSchema` with no errors. `pnpm -r lint && pnpm -r test` green repo-wide (72+52+11+20+... tests across all lanes, including the harness's own 20).
**Done (prior session, unchanged):**
- `testset/README.md`: full collection guide (foil/lighting/angle/blur variety, docs/PRIVACY.md redaction rules, `<id>.jpg`+`<id>.json` naming, how to run the label helper). `testset/strips/` and `testset/bills/` scaffolded (empty, `.gitkeep`).
- `tools/accuracy/src/label.ts`: interactive CLI (`pnpm --filter @asli/accuracy-harness label`) that walks unlabelled images and writes `<id>.json` matching docs/TESTING.md's label shape exactly (Zod-validated via `types.ts`).
- `tools/accuracy/src/run.ts` (`pnpm --filter @asli/accuracy-harness run:accuracy -- --stage <stage> [--method strip|bill] [--upload] [--compare-with <path>]`): resolves the stage's API/Cognito/bucket config from SSM (`aws.ts`), signs in a real test Cognito user (`ASLI_TEST_USER_EMAIL`/`ASLI_TEST_USER_PASSWORD`), uploads each labelled testset image through the real `/v1/uploads` + `/v1/scans`, scores extraction (`score.ts`, using `@asli/matching`'s own normalize/similarity functions so "correct" means the same thing matching does), and separately runs a **seeded tier-correctness probe** (`seededTier.ts`) that builds identities straight from `packages/contracts/fixtures/flagged-batches.json` (the same rows `pnpm seed-fixtures` puts in every stage), predicts each one's tier locally with `@asli/matching`'s `decide()`, then calls the real `/v1/checks` and diffs the two - this catches wiring bugs (bad GSI, wrong stage) without re-testing matching logic itself (that's B's fully unit-tested lane).
- Report (`report.ts`): JSON + markdown per docs/TESTING.md ("Output: `tools/accuracy/out/<timestamp>.json`"), with `--upload` PUTting `latest.json` to `<publicBucket>/metrics/accuracy/latest.json` for J's dashboard. Bedrock token totals are pulled from CloudWatch (`BedrockInputTokens`/`BedrockOutputTokens`, the metrics C already emits) for the run's time window rather than invented; cost-per-1000 is left "unknown" until `packages/contracts/src/pricing.ts` has real verified prices.
- Comparison mode (`--compare-with <prior-report.json>`): prints a before/after table per metric (`compareReports`/`comparisonToMarkdown`) - ready for a strip-vs-bill or prompt-v1-vs-v2 comparison once two real runs exist.
- 20 unit tests (score/testset/report/seededTier/index), all pure/local (temp dirs, no AWS calls) - `pnpm -r lint && pnpm -r test` green repo-wide.
**Remaining:**
- **Human or a future session:** close the strip gap - still 15/30 strips (13 non-Indian but legible, plus the mock-flagged demo strip; only 2 sourced photos are genuinely Indian-market). Real Indian strip photos with a legible batch number are scarce under open licensing (see `testset/sources.md`'s "Coverage gaps" section) - closing this properly likely needs actual human-shot photos per `testset/README.md`, not more internet sourcing. Bills are now 6/10 (see above) and closer to target.
- Acceptance criterion 1 (≥30/≥10) is **not met** (15/30, 6/10) and honestly documented as such.
- Criteria 2 and 3 were met with the 15-strip/1-bill testset (report + a measured comparison in `submission/LEARNING_LOG.md`). **A fresh full run with all 6 bills has not completed yet**: two attempts this session hit real per-user rate limits (`services/scan`'s real 30 scans/hour cap, docs/API.md - both seeded demo users, `asha.demo@asli.internal` and `vikram.demo@asli.internal`, used up their hourly quota mid-run) after also surfacing a real Lambda timeout on one 3-line-item bill scan (see Gotchas below) - a real, not synthetic, finding. A future session (or this one after the hourly quota resets) should just rerun `ASLI_TEST_USER_EMAIL=<either demo user> ASLI_TEST_USER_PASSWORD='AsliDemo!2026' pnpm --filter @asli/accuracy-harness run:accuracy -- --stage int --upload` to get a report over the full current testset.
- Nice-to-have not built: a way to seed a dedicated test-only FlaggedBatches row (task's alternative "or" option) - used the "call `/v1/checks` against the already-seeded fixture rows" option instead since `pnpm seed-fixtures` already seeds `flagged-batches.json` into every stage.
**Gotchas / decisions:**
- `@asli/matching`'s `manufacturerSimilarity(a, b)` expects **already-normalized** strings (its own doc comment says so) - passing raw manufacturer text directly gives a low Jaccard score even for what should be a 1.0 match (e.g. `manufacturerSimilarity('Gidsha Pharmaceuticals', 'Gidsha Pharma')` = 0.33, not 1.0, until both are run through `normalizeManufacturer` first). `score.ts`'s `scoreStrip` does the normalization itself; any other caller of `manufacturerSimilarity` should double check it's normalizing first too.
- Tier-correctness probes are checked against **predictions from the local matching library**, not hand-picked expected tiers, so they stay correct automatically if `packages/contracts/fixtures/flagged-batches.json` changes - but this means they verify *wiring* (does the deployed check return what the pure function would), not matching *logic* (B already owns 100% branch coverage on that).
- Report writer treats missing cost data as the string `"unknown"` rather than `0` or omitting the field, per CLAUDE.md's "measured, not estimated" spirit - don't change this to a fallback of 0.
- **Real Lambda-timeout finding (this session, lane C's `services/scan`, not this lane's to fix):** a bill scan with 3 line items occasionally takes an internal retry (CloudWatch logs show a duplicated `GeminiInputTokens`/`GeminiOutputTokens` array and a "Dimension already added" warning on one such call, total latency 19.5s) - and once, on a second 3-line bill in the same run, that retry pushed total latency past the Lambda's 30s hard timeout, surfacing to the harness as a raw `HTTP 503`. Strips (1 item each) never showed this. Worth lane C knowing about if it recurs; not something E's own harness code can fix.
- `services/scan`'s per-user scan rate limit (`docs/API.md`: 30/hour, `services/scan/src/rate-limit/limiter.ts`) is bucketed by **absolute clock hour** (`floor(epoch/3600)`), not a rolling window from first use - so it resets at the top of the clock hour regardless of when your own run started. Two consecutive full-testset runs (15 strips + up to 6 bills = up to 21 requests each) in the same clock hour, even across two different demo users, can each burn through a meaningful chunk of a 30/hour budget; plan runs accordingly rather than re-running repeatedly to chase a transient failure.
**Contract change requests:**
- none
**Learning log entries added:** yes
