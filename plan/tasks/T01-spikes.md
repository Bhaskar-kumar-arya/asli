# T01 — Spikes: remove external risks
**Priority:** P0 · **Stage:** 0 · **Sessions:** 1 · **Depends on:** T00

## Read first
CLAUDE.md, docs/DATA_SOURCES.md, docs/ALERTS.md, docs/SCANNING.md, docs/PERMISSIONS.md, docs/SAFETY_AND_CONTENT.md (Read-aloud)

## Goal
Answer every "will this work from AWS in ap-south-1?" question in the first hours, with evidence, and write the answers into this file's Handoff and into the named SSM parameters.

## Owns
`spikes/` (throwaway code, kept for the learning log), `infra/lib/lanes/t01-spikes.ts` (deleted or disabled at the end)

## Spikes
1. **CDSCO endpoint from Lambda (ap-south-1).**
   - Call `filteredNsqDrugTable` for 3 months, with and without: browser User-Agent, `Referer` of the public NSQ page, `X-Requested-With: XMLHttpRequest`, cookies obtained by first loading the parent page.
   - Find the Spurious tab value and `publicReportingMonths` exact parameters from the page's network requests (use a browser dev tools session and record the requests).
   - Record: status codes, the minimum request that works, response format (HTML fragment/JSON), columns, date formats, pagination, row count for one month. Save 3 sample responses to `packages/contracts/fixtures/cdsco/` (hand to T02).
   - **Verdict:** `ENDPOINT_OK` or `ENDPOINT_FAILS` (then A3 becomes P0).
2. **PDF listing.** Find the listing page URL for NSQ alerts and confirm links can be discovered; download 2 PDFs from Lambda. Save to fixtures.
3. **Bedrock.** Which Claude vision models are invokable from ap-south-1 (direct or via an inference profile)? Enable model access. Run one strip image through Converse with a tool schema. Write the model/profile ID to SSM `/asli/dev/bedrock/visionModelId` and in Handoff.
4. **Web push to Android.** Minimal page + service worker on the Amplify URL, VAPID keys, one push from a Lambda to a real Android Chrome. Note iOS limitations.
5. **SES.** Verify sender address/domain and the team's demo recipient emails; submit production access request; record status.
6. **Verified Permissions.** Create a policy store in ap-south-1, load the schema from docs/PERMISSIONS.md, run one IsAuthorized call with inline entities.
7. **Polly and Translate.** List voices for `hi-IN`, `en-IN`, `kn-IN` (engine support). Translate one template to `hi` and `kn`.
8. **Textract.** Run AnalyzeDocument (TABLES) on one CDSCO PDF page; note quality of batch number cells.

## Acceptance criteria
- [ ] Each spike has a result, evidence (status codes, IDs, screenshots path) and a decision in Handoff
- [ ] Endpoint verdict written, and human told immediately if `ENDPOINT_FAILS`
- [ ] Fixture files saved for T02
- [ ] SES production access requested
- [ ] Learning log has at least 3 entries

## Out of scope
Production-quality code.

---
## Handoff (the session updates this before stopping)
**Status:** BLOCKED (on human AWS-console actions - every spike that could be run without them is done; see Remaining)
**Stage deployed:** none (spikes are throwaway scripts run directly against the account, not deployed to a stage)
**Done:**
- **Spike 1 - CDSCO endpoint: `ENDPOINT_OK`, and better than expected.** A plain `curl` with **no** browser User-Agent, Referer, X-Requested-With, or cookies returns HTTP 200 with real data from `GET https://cdscoonline.gov.in/CDSCO/filteredNsqDrugTable?month=Feb-2026&source=All&tab=nsq` - 217 rows for Feb-2026. The `tab` param does **nothing**: `tab=spurious`/`Spurious`/`SPURIOUS`/anything all return identical NSQ data. Spurious drugs are a **separate endpoint**: `GET /CDSCO/filteredSpuriousDrugTable?month=Feb-2026&source=All` (4 rows for Feb-2026, different field names - see fixtures README). Response is DataTables-style JSON (`{iTotalDisplayRecords, iTotalRecords, aaData: [...]}`) despite `Content-Type: text/plain;charset=ISO-8859-1`. Found the real months-listing endpoint by trial: `GET /CDSCO/reportingMonths?year=<YYYY>` → `["Jan",...]` (docs/DATA_SOURCES.md's guessed name `publicReportingMonths` returns HTTP 400 - **wrong path**, worth fixing in that doc). 2019-2025 all return full 12 months; 2018 returns `[]`, so 2019 is the earliest practical backfill year. DATA_SOURCES.md's "known risk" of a 400 from a non-browser fetch did **not** reproduce today - either transient, or CDSCO changed something since planning. 3 real sample responses + 2 real PDFs saved to `packages/contracts/fixtures/cdsco/` (see that folder's README for exact request details) - **on this branch (`lane/T01`), not yet merged**, T02 needs to pull these in.
- **Spike 2 - PDF listing: done.** Found via web search (the listing page `https://cdsco.gov.in/opencms/opencms/en/Notifications/nsq-drugs/` renders its PDF table with JS, so a plain fetch doesn't show links - a human with a real browser should confirm the in-page discovery flow works, I only confirmed the PDFs are directly downloadable once you have the URL). Downloaded 2 real PDFs (March 2025, Sept 2024) from `cdsco.gov.in/opencms/resources/UploadCDSCOWeb/2018/UploadAlertsFiles/...`, no auth needed, both valid 9-page PDFs. Saved to fixtures.
- **Spike 3 - Bedrock: partially done, blocked on a human console action.** `ListFoundationModels` (control-plane, read-only) works and lists every Claude model with IMAGE+TEXT modalities, including the newest ones (Claude 5 family, Haiku 4.5) each with an `ACTIVE` cross-region inference profile (`global.anthropic.claude-haiku-4-5-20251001-v1:0` etc.) - full list in `spikes/bedrock.ts` output. **But every actual `Converse`/invoke call fails** with `ValidationException: Operation not allowed`, for both `global.*` and `apac.*` profiles and even the old on-demand `anthropic.claude-3-haiku-20240307-v1:0` - consistent with **model access not yet granted** for this account in this account/region, which is a one-time manual step in the Bedrock console (Model access page, accept Anthropic's EULA) that I cannot do via API. **Recommendation once unlocked:** `global.anthropic.claude-haiku-4-5-20251001-v1:0` (cheapest current vision-capable model, ACTIVE profile) - but this is **unverified**, not yet actually invoked successfully. Also: `testset/` has 0/30 strip photos collected yet (per its own README and plan/INTEGRATION_LOG.md lane E row), so even with model access granted there's no real strip photo to test extraction accuracy against yet - `spikes/bedrock-converse.ts` only did a text-only tool-forcing smoke test (Converse + `toolChoice` + `temperature:0` mechanics), not real vision extraction.
- **Spike 5 - SES: mostly done, needs a human decision.** `GetAccount` works: `SendingEnabled: true`, `EnforcementStatus: HEALTHY`, but `ProductionAccessEnabled: false` (sandbox, 200/day, 1/sec) and 0 verified identities. SES itself is healthy and not affected by the account-verification issue below. **Did not** verify a sender identity or submit the production-access request, because that needs the human to pick and have mailbox/DNS access to a real sender address or domain (`alerts@<what?>`) and list the team's actual demo recipient emails (sandbox mode can only send to verified recipients) - I have no way to receive a verification email or add a DNS TXT record from here.
- **Spike 6 - Verified Permissions: blocked, same root cause as Bedrock.** `CreatePolicyStore` fails immediately: `AccessDeniedException: The AWS Access Key Id needs a subscription for the service`. Cedar schema/policy JSON is written and ready to go (`spikes/avp.ts`, mirrors `infra/lib/shared-stack.ts`'s `AVP_SCHEMA`/`AVP_POLICIES` T02 already built) - just never got to run it.
- **Spike 7 - Polly and Translate: Polly done (bad news), Translate blocked.** `DescribeVoices` (works fine, not blocked) shows **zero voices for `hi-IN` and zero for `kn-IN`** - only `en-IN` has voices (Raveena/Aditi standard, Kajal neural). Checked the full 100-voice catalog for anything Hindi/Kannada by language name too, not just the `-IN` code - nothing. **This contradicts docs/SAFETY_AND_CONTENT.md's assumption that Hindi read-aloud uses pre-rendered Polly MP3s** - it doesn't have the option; Hindi needs the same browser-`speechSynthesis` fallback the doc only planned for Kannada. `TranslateText` fails: `SubscriptionRequiredException: The AWS Access Key Id needs a subscription for the service` - same pattern as Verified Permissions and Textract.
- **Spike 8 - Textract: blocked, same root cause.** Rasterized page 1 of the March-2025 PDF locally with `pymupdf` (available in this environment; no `pdftoppm`/ImageMagick installed) since sync `AnalyzeDocument` needs an image, not a PDF. `AnalyzeDocument` with `FeatureTypes: ['TABLES']` fails: `SubscriptionRequiredException: The AWS Access Key Id needs a subscription for the service`.
- **Spike 4 - Web push: not attempted.** Needs a page + service worker actually served from the Amplify URL (not connected yet - T00 Handoff) and a real Android Chrome to receive the push. Nothing meaningful to test from this environment; VAPID key generation itself is already built and unit-testable in T02's `scripts/vapid-keys.ts`.
- Throwaway spike scripts at `spikes/*.ts` (own `package.json`, not a pnpm workspace member on purpose) for every spike above; kept for the learning log per this task's "Owns".
**Remaining (needs the human):**
- **Grant Bedrock model access** in the AWS console (Bedrock → Model access) for Anthropic models in whichever region/profile scope applies (`ap-south-1` direct and/or the `global`/`apac` cross-region profiles) - this is the single biggest blocker, since scanning (the core feature) needs it. Re-run `spikes/bedrock-converse.ts` after to confirm, then update SSM `/asli/<stage>/bedrock/visionModelId` (currently T02's placeholder `PLACEHOLDER_PENDING_T01`) with the real chosen model/profile ID.
- **Check on the "account is currently being verified" AWS message** from Bedrock's *first* error today (it changed to a different error - "Operation not allowed" - on retry ~15 min later, so that part may have already cleared) - if `SubscriptionRequiredException` on Translate/Textract/Verified Permissions doesn't clear on its own soon, it may need the same kind of account-level action as Bedrock, or a support case. Re-run `spikes/avp.ts`, the Translate half of `spikes/polly-translate.ts`, and `spikes/textract.ts` after either enough time has passed or after taking action.
- **Pick a real sender identity/domain and the team's demo recipient emails for SES**, verify them (console or `VerifyEmailIdentityCommand`/DNS TXT record - needs mailbox/DNS access I don't have), then submit the SES production-access request. SES itself is otherwise ready.
- **Confirm PDF link discovery in a real browser** on `https://cdsco.gov.in/opencms/opencms/en/Notifications/nsq-drugs/` (its table is JS-rendered, so I only confirmed direct PDF URLs work once you have them, not the in-page listing/discovery flow A3 will need to automate).
- **Web push spike (4) needs a real Android phone** once Amplify Hosting is connected (T00 blocker) - nobody's done this yet.
- Once Bedrock/Textract/Translate/AVP are unblocked, actually run the real vision-extraction and IsAuthorized checks (this session only reached "service reachable" / mechanics-work confirmation for Bedrock's tool-forcing and AVP's schema+policy correctness, not a live pass).
- **Fix docs/DATA_SOURCES.md**: the `publicReportingMonths` name it guesses is wrong (returns 400); the real endpoint is `GET /CDSCO/reportingMonths?year=<YYYY>`. I didn't edit docs/ myself (out of this task's "Owns" list) - flagging for whoever can.
**Gotchas / decisions:**
- This worktree (`../asli-T01`, branch `lane/T01`) was branched from `lane/T02`'s tip (not `main`), because that was `HEAD` in the shared session when `scripts/new-worktree.sh T01` ran. It carries T02's two commits in its history. Not broken - X can still merge both normally, lane/T02 first - but worth knowing before anyone tries to diff `lane/T01` against `main` and gets confused by unrelated T02 changes showing up.
- `spikes/` has its own `package.json` (plain `npm install`, not part of `pnpm-workspace.yaml`) since these are explicitly throwaway per this task's "Owns" - didn't want AWS SDK clients only used once polluting the real workspace's lockfile.
- All AWS calls in this session ran directly from this local dev environment with the account's own credentials, not "from a Lambda in ap-south-1" as the task literally says - `cdk deploy` (which would be needed to actually run something in Lambda) is blocked by this session's own tool-permission layer as a "Blind Apply" action. Functionally equivalent for testing account/region capability (same account, region pinned via SDK client config), but doesn't test Lambda-specific network path quirks. Worth a real from-Lambda check once deploys are unblocked, especially since DATA_SOURCES.md's originally-reported 400 didn't reproduce here and Lambda's egress path could plausibly behave differently.
- Four different services (Bedrock invoke, Verified Permissions, Translate, Textract) all failed today, but with two distinctly different error shapes (Bedrock's own "account verification" AccessDeniedException that then changed to a ValidationException on retry, vs. the other three's stable `SubscriptionRequiredException`) - treating these as two separate blockers above rather than assuming one fix resolves everything.
**Contract change requests:**
- none
**Learning log entries added:** yes
