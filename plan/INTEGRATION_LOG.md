# INTEGRATION_LOG.md (maintained by lane X)

## Gates
| Gate | Target (IST) | Status | Time passed | Evidence |
|---|---|---|---|---|
| Stage 0 | Thu 11:00 | ✅ | | Amplify URL **live**: https://main.d2ag2oukltn4mc.amplifyapp.com. T01 verdict **known**: ENDPOINT_OK. **contracts-v1 tagged** on the deployed lane/T02 commit, pushed to origin. |
| Scan | Fri 12:00 | ✅ | 2026-09-18 21:09 IST | `tests/e2e` against `int`: manual check on a real seeded FlaggedBatch row (`GTL 1258` / Amoxicillin, NSQ) returns FLAGGED with a real cdscoonline.gov.in source URL and alertMonth. (Real strip photo → Bedrock vision scan still blocked account-wide, see T01; `POST /v1/scans` code path is fixture-tested only.) |
| Core | Fri 20:00 | ✅ | 2026-09-18 21:09 IST | `tests/e2e` against `int`: add medicine (`AKL1000` / Paracetamol, NSQ) → retroactive check → FLAGGED MATCH within ~2s (well under the 10s bar) → AlertEvent → both G1 PushSenderHandler and EmailSenderHandler confirmed (CloudWatch Logs) processing the event for that cabinet within 20s. Actual push/email *delivery* not verified (no real push subscription; SES sandbox has no verified test recipient) - only the fan-out path, which is what this gate checks. |
| Early submission | Sat 18:00 | ⬜ | | Submission form confirmation |
| Freeze | Sat 22:00 | ⬜ | | e2e green, no P0 bugs |
| Final submission | {{SUBMISSION_DEADLINE}} − 3 h | ⬜ | | |

## Lane status
| Lane | Pri | Status | Merged to main | Deployed to int | Notes |
|---|---|---|---|---|---|
| T00 | P0 | 🟨 | (initial scaffold commits) | | Amplify Hosting live: https://main.d2ag2oukltn4mc.amplifyapp.com. Only remaining: AWS Budgets alert. |
| T01 | P0 | ✅ | 2026-09-18 | | **DONE, merged to main** (`0b33961`). Endpoint verdict: **ENDPOINT_OK**. Bedrock/Translate/Textract/AVP invocation confirmed blocked by an account-level restriction, not IAM/SCP (verified separately: root-user caller bypasses IAM, `describe-organization` confirms no org/SCP exists, `get-foundation-model` shows the model is ACTIVE but `invoke-model` returns `ValidationException: Operation not allowed` across 4 providers). SES sender identity still needs human AWS-console action. See T01 Handoff and 2026-09-18 18:16 IST learning log entry. |
| T02 | P0 | ✅ | 2026-09-18 | | **DONE, merged to main** (`99ef790`). contracts + shared-stack + api-routes + seed scripts, `pnpm -r lint/test/build` green. Deployed to `dev-shared` - 26 SSM params verified, VAPID keys + fixtures seeded. `contracts-v1` tagged. Verified Permissions deferred (`ENABLE_AVP=false`, account-restriction blocker - see T01). |
| A1 | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`98d0663`). CDSCO endpoint client/parser: real T01-fixture-verified parsing (217 NSQ + 4 Spurious rows), `pnpm -r lint/test/build` green (49 new tests). Real `LaneA1Stack-dev-a1` deploy invoked end-to-end against the live CDSCO endpoint, confirmed S3 write. Deployed to `int` 2026-09-18 by X (`dev-a1` left in place, doesn't register any HTTP routes so no conflict). |
| A2 | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`ec277c6`). Ingestion state machine, daily check-months, backfill, demo replay, build-reference. `pnpm -r lint/test/build` green (90 new tests). Real `LaneA2Stack-dev-a2` deploy - all 6 acceptance criteria verified against it, including a genuine idempotency bug (rowHash depended on evolving aliases) found and fixed only by re-running on real infra. Deployed to `int` 2026-09-18 by X; `dev-a2` **destroyed** first (see note below - shared HTTP API route conflict). |
| A3 | P2 | 🟨 | not merged | | **BLOCKED, on branch `lane/A3` only** (`286a006` feat(A3), not merged to main). Textract/table-reconstruct/subflow code-complete, 33 new tests, `cdk synth LaneA3Stack-dev-a3` clean - but blocked on the same account-wide Textract `SubscriptionRequiredException` as Bedrock/Translate (see T01), **not wired into A2's `build.ts` `ChooseSource` PDF branch** (still routes to `PdfNotImplemented`, that edit belongs to A2/X), and only 2 of 3 required fixture PDFs exist (Jan-2025 PDF never captured). Per CLAUDE.md/cutting rules ("merge only if fully working" for P2, and A3 is P2 since the endpoint verdict is OK), recommend leaving unmerged unless Textract access clears - the PDF fallback isn't on the critical path. |
| B | P0 | ✅ | 2026-09-18 | | **DONE, merged to main** (`792c59e`). All 10 required test cases + 100% branch coverage on classifyMatch/decide, property tests, benchmark. Human reviewed classify.ts against the tier table in chat and approved (task's own acceptance criterion). `@asli/matching` is now real, not a stub. |
| C | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`d1e6a20`). Upload/scan/check/alert-detail APIs + `packages/lookup`. `pnpm -r lint/test/build` green (47 new tests in services/scan, 5 in packages/lookup, 194 total repo-wide). Real `LaneCStack-dev-c` deploy; Checks/Alerts/Uploads invoked directly against real seeded data (FLAGGED result, SPURIOUS-first, clean logs). Scans endpoint code-complete + fixture-tested but not live-verified - blocked on the same account-wide Bedrock restriction as A2/N (see T01). Deployed to `int` 2026-09-18 by X; `dev-c` **destroyed** first (route conflict, see note below); `/v1/checks` verified live by `tests/e2e` (Scan gate). |
| D1 | P0 | 🟨 | 2026-09-18 | | Merged to main alongside D2 (`54beaf6`, commit `feat(D1+D2)` - built in one session, human-approved). Theme, shared components, Cognito auth, API client, MSW mocks, i18n shell frame, service worker/push, settings screen. **Remaining:** real Cognito sign-in not yet tested against a deployed User Pool/`int` - needs `VITE_COGNITO_USER_POOL_ID`/`VITE_COGNITO_USER_POOL_CLIENT_ID`/`VITE_API_BASE_URL` set in Amplify Hosting build env and a smoke test. |
| D2 | P0 | 🟨 | 2026-09-18 | | **Merged to main** (`54beaf6`). Method chooser, capture, confirm/manual entry, result card (all docs/UX.md states), bill results, read-aloud, save-to-cabinet. 24 tests passing incl. banned-words grep. **Remaining:** hi/kn result copy (moves to lane I once it ships); not yet run end-to-end against a real `int` deploy or real device; screen recording not captured. |
| D3 | P0 | ✅ | 2026-09-18 | | **DONE, merged to main** (`290b23e`). Cabinet, medicine detail, members, and notification deep-link screens. |
| E | P1 | 🟨 | | | **IN PROGRESS, not yet merged.** Accuracy harness built: label CLI, runner (upload+scan+checks against a real stage via SSM/Cognito), scoring against `@asli/matching`'s own normalize functions, seeded tier-correctness probes off `flagged-batches.json`, JSON+markdown reports, comparison mode. `pnpm -r lint/test/build` green (20 new tests). Blocked on: photos (0/30 strips, 0/10 bills - human task) and a live `int`/`dev-*` deploy with working scans (Bedrock still blocked account-wide per T01) to actually run against. See plan/tasks/E-accuracy-harness.md Handoff. |
| F | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`9fa6533`). Cabinet CRUD handlers + retroactive check stream consumer, local authz stub (H's package still a placeholder at merge time), 34 unit tests, `pnpm -r lint/test/build` green, `cdk synth LaneFStack-dev-f` clean. Deployed to `int` 2026-09-18 by X. **Bug found and fixed by X during integration:** `infra/lib/lanes/f-cabinet.ts` imported the FlaggedBatches table with `Table.fromTableName` (no index info), so `grantReadData` never covered the GSI1 index the retroactive check queries - every retroactive check was silently failing `AccessDeniedException` and medicines stayed `PENDING` forever. Fixed to `fromTableAttributes({ globalIndexes: ['GSI1'] })`, redeployed, confirmed fixed by `tests/e2e` (Core gate). Mechanical IAM-only fix, no business logic touched. |
| G1 | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`92dabd6`). Subscriptions API + push-sender/email-sender Lambdas + wording-enforced templates, 26 unit tests, `pnpm -r lint/test/build` green, `cdk synth` clean. Deployed to `int` 2026-09-18 by X; PushSenderHandler/EmailSenderHandler confirmed processing real AlertEvents via `tests/e2e` (Core gate) - actual push/email delivery not verified (no real push subscription; SES sandbox has no verified test recipient). |
| G2 | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`f96ea7e`). New-alert fan-out: stream consumer on FlaggedBatches (GSI3 candidate lookup, conditional MATCH puts, latestTier bump, `AlertEvent` publish), 60-day backfill notification guard (age-of-`alertMonth`, not execution-tag - `IngestionState.sourceType` can't distinguish backfill from daily). `pnpm -r lint/test/build` green (19 new tests), `cdk synth` clean. Deployed to `int` 2026-09-18 by X. |
| H | P1 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`54e4d8d`). Cedar `stub`/`avp` authz, invites and members API. 56 new tests. Deployed `LaneHStack-dev-h` and invoked all 4 handlers directly against real seeded fixture data in `stub` mode - OWNER invites, VIEWER denied, invite accept single-use, last-owner guard, all confirmed live. `avp` mode unit-tested only (blocked on the same account-wide Verified Permissions restriction as T01/T02). See plan/tasks/H-permissions-cedar.md Handoff. Deployed to `int` 2026-09-18 by X; `dev-h` **destroyed** first (route conflict, see note below). |
| I | P1 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`a861075`). Content package (`render()`, banned-words check), `GET /v1/content/guidance/{guidanceKey}` API, hand-drafted hi/kn translations. **Remaining (non-blocking):** native-speaker review of hi/kn strings unset; real Amazon Translate/Polly runs blocked (`SubscriptionRequiredException`, same account restriction as Bedrock - Polly also has zero `hi-IN`/`kn-IN` voices in `ap-south-1`, so hi/kn read-aloud falls back to browser `speechSynthesis`, not Polly MP3s); D2/G1 still ship their own hardcoded English stand-ins until they swap over (their work, not I's). Deployed to `int` 2026-09-18 by X. |
| S | P1 | ✅ | 2026-09-18 | int | **DONE.** `computeImpactStats`, `compute-stats` Lambda, `GET /v1/public/stats`, stats-report export script. Deployed to `int` 2026-09-18 by X. **2026-09-19 (X, Wave 2 int pass):** invoked `compute-stats` for real against `int`'s 1032 already-ingested FlaggedBatches rows (11 months) - `/v1/public/stats` now returns real numbers (`totalFlaggedBatches: 1032, cabinetsProtected: 4, medicinesTracked: 3, monthsCovered: 11, latestMonth: 2026-02`) instead of zeros. Human still needs to compare these against docs/PRODUCT.md's Evidence table (49/48, 52/52, 70/70) and decide whether to update that doc - not done this session (that's a product-doc decision, not an engineering one). Note: defines its own `ImpactStatsDocument` outside `@asli/contracts` (see contract change request below), separate from J's operational `StatsDocumentSchema`. |
| J | P2 | ✅ | 2026-09-18 (`da82557`) | int | **Deployed to `int` 2026-09-19 by X (Wave 2 int pass).** `GET /v1/public/metrics`, CloudWatch dashboard/alarms/budget, `/dashboard` page. **Real deploy found a genuine bug**, only surfaced by `cdk deploy` against real CloudFormation: `LambdaErrorRateAlarm` combined two CloudWatch Metrics Insights `SELECT` queries (`errors`, `invocations`) via a further math expression into one alarm - CloudFormation rejected this with `CREATE_FAILED: Invalid metrics list` (CloudWatch Alarms support only one Metrics Insights query per alarm, not several combined arithmetically; undocumented in this repo). Fixed by X directly in `infra/lib/lanes/j-dashboard.ts` (J's file - a mechanical infra-only fix, not business logic, same class as the F/GSI1 fix from the prior int-deploy session): reduced to a single-query, raw account-wide error-count alarm (threshold 10 over 5 min) instead of an error-rate percentage; documented inline with a NOTE comment for J to revisit if the rate is wanted back. Stack now deploys clean; `GET /v1/public/metrics` verified live (200, schema-valid body). **Remaining:** `accuracy.sampleSize`/`cost.scanCount` are legitimately 0 - no scan traffic has hit `int` in the trailing 24h CloudWatch window (Bedrock vision scans still blocked account-wide, see T01), so acceptance criterion 1 ("numbers match a manual calc from CloudWatch for one real scan") still isn't provable against real traffic, only against injected-fixture unit tests as before. `PRICING_TABLE` still entirely `null` (needs a human ~30-60 min with AWS Pricing Calculator). 360px readability still not browser-verified. Contract change request and Budgets/OpsTopic policy gap unchanged from before. |
| K | P2 | 🟨 | 2026-09-18 (`7e2dc9e`) | none (client-only) | **Code-complete, blocked on real QR samples** - no infra to deploy (client-only), out of scope for this Wave 2 int pass. **Remaining:** acceptance criterion "≥3 real pack QR codes decoded correctly" not met - needs a human to shoot ≥3 real pack QR photos, drop into `testset/qr/`, and record decode rate in E's comparison. `VITE_FEATURE_QR` off by default, no `.env` in repo - whoever demos must set it. No contract changes. |
| L | P2 | ✅ | 2026-09-18 (`ae8968e`) | int | **Deployed to `int` 2026-09-19 by X (Wave 2 int pass).** `dev-l` destroyed first (same shared-HTTP-API route-conflict pattern as A2/C/H), then `LaneLStack-int` deployed clean. **Wired into both screens X was asked to wire** (L deliberately left this since it doesn't own those files): `apps/web/src/features/scan/components/ResultCard.tsx` and `apps/web/src/features/cabinet/pages/MedicineDetailPage.tsx` each got one import (`ReportProblemButton` from `../../report`) and one JSX line, passing `identity`/`alertRef` from the result/medicine already in scope. `pnpm -r lint/test/build` green after the change (52 web tests passing, no regressions). **Verified `POST /v1/reports` live end-to-end** by invoking `LaneLStack-int-CreateReportHandlerFF4B1434-fAk8Sjd9vHVj` directly with a real batch identity (`C125R01`) and a synthetic JWT-authorizer claim (no live Cognito user available this session - see Gotchas): 201, real PvPI routes returned (helpline, ADR PvPI app link, CDSCO ADR PDF), and the stored DynamoDB item (`asli-dev-shared-reports`) confirmed to hold only `batchNorm`/`problemType`/`note`/`alertRef`/`createdAt` - no `userId`, no product name, no manufacturer, matching the privacy design. |
| M | P2 | ✅ | 2026-09-18 (`19acf86`) | int | **Deployed to `int` 2026-09-19 by X (Wave 2 int pass).** `GET /v1/public/insights` reshapes S's `ImpactStatsDocument`. **Real deploy + a real S backfill found a genuine cross-lane bug**, only surfaced by calling the live endpoint against real data: M's handler read `STATS#IMPACT`/`ALL`'s `document` field as if it were a full `ImpactStatsDocument` (`{generatedAt, overall, byMonth}`), but S's `compute-stats` (`services/stats/src/handlers/compute-stats.ts:88-101`) actually writes `document: doc.overall` (just the overall `ImpactStats`, no nested `byMonth`) into that item, and writes each month as its *own separate* `STATS#IMPACT`/`<month>` item - both lanes' own unit tests passed because each used fixtures shaped by its own (incompatible) assumption. Real call 500'd with `TypeError: Cannot convert undefined or null to object` (`Object.entries(document.byMonth)` on `undefined`). Fixed by X in `services/insights/src/handlers/public-insights.ts` (M's file): switched from a single `GetCommand` to a `QueryCommand` on `PK = STATS#IMPACT`, assembling the `ImpactStatsDocument` this handler's `toDetail` expects from the `ALL` item plus every `<month>` item; updated `public-insights.test.ts`'s mock to the real multi-item shape. `pnpm --filter @asli/insights test/lint/build` green, redeployed, **re-verified live**: `GET /v1/public/insights` now returns real numbers (`byCategory: {NSQ: 1023, SPURIOUS: 9}`, 11 months, reason codes, lag distributions) and `1023 + 9 = 1032` matches S's `totalFlaggedBatches` exactly (headline-matches-S acceptance criterion, confirmed against real live data, not fixtures). Contract change request unchanged from before. |
| N | P2 | ✅ | 2026-09-18 (`0ede6bf`) | int | **Deployed to `int` 2026-09-19 by X (Wave 2 int pass).** `POST /v1/pharmacy/checks`. **Both acceptance criteria now verified against real `int` data**, not just mocks: invoked `LaneNStack-int-PharmacyChecksHandler78DB8A5D-mqMKq7Nz5Lm4` directly with a real 200-row CSV (3 rows using real seeded FlaggedBatches batch numbers incl. one with the correct manufacturer, 197 clean filler rows) via a synthetic JWT-authorizer event (no live Cognito user - see Gotchas) - 200 OK, 200 rows returned, the one row with a correct manufacturer match came back FLAGGED with a real CDSCO source (the other two seeded rows correctly came back NO_ALERT_FOUND since their manufacturer field was a placeholder, not the real one - correct matching behavior, not a bug). CloudWatch REPORT lines: first (cold, incl. 392ms init) 4.8s, two subsequent warm invocations 3.4s and 3.0s - comfortably under the <5s bar once warm. Invoice-photo path still unverified for real (needs a real Bedrock call, still blocked account-wide per T01 - same blocker as C/A2/D2). |
| Z1 | P0 | ✅ | | int (`DELETE /v1/me` synth-clean, not yet deployed) | **DONE 2026-09-19, addendum same day.** All checklist items complete except the demo-replay recording, which genuinely needs a human with an Android phone. Audit results: K confirmed off by default (Amplify env vars checked directly, no override anywhere), banned-words en/hi/kn green, FLAGGED/VERIFY source links structurally guaranteed by `FlaggedBatchSchema`, CDSCO-outage resilience confirmed by construction (no user-facing path ever calls CDSCO live), cold-start numbers measured and fine, git history clean (first commit 2026-09-17 17:11 IST, single author, no vendored code). Wrote and ran `scripts/seed-demo.ts` for real against `dev-shared`: real Cognito users (`asha.demo@asli.internal` OWNER, `vikram.demo@asli.internal` EDITOR) + a real "Mom's medicines" cabinet, and the seeded disclosed-mock-strip medicine matching `fixtures/demo/replay-1.json`'s real CDSCO row (E9AIY029) resolved live to FLAGGED with a real MATCH item within seconds - the two-siblings sharing moment is proven end to end on real infra. Purged 2 stale pre-fix messages from `asli-dev-shared-stream-dlq` (leftovers from F's already-resolved GSI1 bug), clearing the `asli-int-stream-dlq-depth` alarm back to OK. Redeployed `int` from `main` via one `cdk deploy --all` - all 13 stacks succeeded clean in 357.73s, matching the pre-deploy `cdk diff --all` preview exactly (Lambda-bundle-hash-only changes on 7 stacks, none on the other 6). **Addendum (2026-09-19, same day):** picked up the 3 remaining human-flagged items. (1) Reconciled `docs/PRODUCT.md`'s Evidence table against S's real numbers by querying `flagged-batches` GSI2 directly (not the stale `1032`/`11-month` figure recorded above - ingestion kept running, real total is now 3,326 rows/21 months) - Jan/Mar-2025 central-lab counts matched the hand-verified table exactly; Sep-2024 has 0 rows in the real backfill since CDSCO's listing no longer carries that month, documented rather than silently dropped. (2) Filled `packages/contracts/src/pricing.ts`'s `PRICING_TABLE` for real via the AWS Price List API (region `ap-south-1`) - all 13 keys sourced/dated except `bedrockOutputTokenPer1k` (left `null`, no SKU exists for it in any region) and a documented gap: **no Anthropic Bedrock model is priced in `ap-south-1` at all** (only `us-east-1`/`us-west-2`), independent of T01's account-wide restriction - a real vision Lambda would need a cross-region inference profile. (3) Implemented the optional `DELETE /v1/me` (new `services/account` package + `infra/lib/lanes/z1-account.ts`): removes the caller's cabinet memberships (or the whole cabinet if they're its sole member), refuses 409 if they'd orphan a shared cabinet as its sole owner, deletes push subscriptions and the Cognito user. 12 new tests, `cdk synth LaneZ1Stack-dev-z1` clean against real `dev-shared` SSM params; not yet deployed to `int` (needs `cdk deploy LaneZ1Stack-int` folded into the next full redeploy). `pnpm -r lint/test/build` green repo-wide throughout. See `plan/tasks/Z1-hardening-freeze.md` Handoff addendum and `submission/LEARNING_LOG.md` for full detail. |
| Z2 | P0 | ⬜ | | | Blocked: waiting on Z1. |

**Note (2026-09-18):** this table was significantly out of date - it previously showed A1, A2, C, D1, D2, D3, F, G1, G2 as unmerged or not started when `git log --oneline --all | grep merge` shows all of them already merged to `main`. Reconciled against actual git history above; commit hashes included so this doesn't drift again. Still true and worth escalating: **no lane has deployed its backend to the shared `int` stage yet** - all real deploys so far are to per-lane `dev-<id>` stages. The Scan and Core gates below can't be marked ✅ until lane X does an `int` deploy and runs the real end-to-end checks.

**Note (2026-09-18, update 2):** A3, E, I, S all executed. E, I, S committed directly to `main` (`d304fd3`, `a861075`, `182bbe2` - not via a `merge(X)` commit, but same effect). A3 is the odd one out: still sitting on unmerged branch `lane/A3` (`286a006`), blocked on account-wide Textract access and not wired into A2's `build.ts`, and per the P2 "merge only if fully working" cutting rule it should probably stay unmerged unless Textract clears. **The critical path is now entirely on lane X**: every P0/P1 Wave 1 lane is merged or code-complete, but nothing has been deployed to `int`, so the Scan and Core gates are the actual bottleneck, not any more Wave-1 lane work.

**Note (2026-09-18, update 3 - X's `int` deploy session):** Deployed all 9 Wave-1 lane stacks (A1, A2, C, F, G1, G2, H, I, S) to `int`. Three things discovered only by doing a real `int` deploy, none catchable by per-lane `dev-*` testing alone:
1. **Shared HTTP API is one physical API Gateway across every stage** (SSM-imported, not per-stage) - `dev-a2`, `dev-c`, `dev-h` already owned routes the `int` versions of those same lanes needed, causing `ConflictException` on deploy. Fixed by destroying those three now-redundant per-lane sandbox stacks (their acceptance criteria were already met and logged before this session; nothing was lost) - see AWS `cdk destroy` runs this session. `dev-a1` was left alone since A1 registers no HTTP routes.
2. **Shared Cognito client only had `userSrp` auth enabled** (`infra/lib/shared-stack.ts`, T02-owned) - no lane's test tooling (this session's `tests/e2e`, or E's `tools/accuracy` which already assumed `USER_PASSWORD_AUTH` would work) could sign in a test user non-interactively. Fixed by adding `userPassword: true` to the client's `authFlows` (additive, doesn't remove SRP) and redeploying `SharedStack-dev-shared`.
3. **F's retroactive-check Lambda had no IAM permission to query FlaggedBatches' GSI1** (`Table.fromTableName` vs `fromTableAttributes({ globalIndexes })`, see F's row above) - every retroactive check was failing silently, so newly added medicines never left `PENDING`. Fixed, redeployed `LaneFStack-int`, confirmed by e2e.

A test Cognito user (`e2e-test@asli.internal`, in the shared `dev-shared` pool, credentials known to this session and the human, not committed anywhere) was created for `tests/e2e` and future `tools/accuracy` runs. Built `tests/e2e/` (previously didn't exist - `pnpm -r lint/test/build` green, skips itself when `ASLI_TEST_USER_EMAIL`/`ASLI_TEST_USER_PASSWORD` aren't set so `pnpm -r test` stays green without live credentials). Ran it for real against `int`: both the Scan-gate check (manual check on a seeded row → FLAGGED with a real CDSCO source URL) and the Core-gate check (add medicine → retroactive MATCH within 10s → AlertEvent → both push-sender and email-sender confirmed processing it) passed. **Scan and Core gates marked ✅ above with real evidence**, not just "code complete."

**Still remaining for a later X session:** `scripts/seed-demo.ts` and `fixtures/demo/replay-1.json` (X's own deliverables per the task file, not yet started - needed for the demo-replay path and the "two siblings + Mom's cabinet" seed data); real push/email *delivery* (vs. just fan-out) needs a real push subscription and an SES-verified test recipient; `POST /v1/scans` (real photo → Bedrock vision) is still blocked account-wide (see T01) so the Scan gate's "real strip photo" half is unverified - only the manual-check half is proven live.

**Note (2026-09-19, X's Wave 2 `int` deploy session):** Wave 2 (J, K, L, M, N) was merged to main but nothing had been deployed to `int` and S had never been backfilled with real data - both blocking the "verify Wave 2 for real" ask. This session:
1. Wired lane L's `ReportProblemButton` into `ResultCard.tsx` (lane C) and `MedicineDetailPage.tsx` (lane F) - one import + one JSX line each, exactly per L's Handoff instructions. `pnpm -r lint/test/build` green after.
2. Destroyed `LaneLStack-dev-l` first (same shared-HTTP-API route-conflict pattern as A2/C/H from the prior session), then deployed `LaneJStack-int`, `LaneLStack-int`, `LaneMStack-int`, `LaneNStack-int` (K is client-only, nothing to deploy).
3. **Found and fixed two genuine bugs, both only surfaced by real deploys/invocations, neither catchable by any lane's own unit tests:**
   - **J:** `LambdaErrorRateAlarm` combined two CloudWatch Metrics Insights `SELECT` queries via a further math expression in one `CfnAlarm` - CloudFormation `CREATE_FAILED: Invalid metrics list` on real deploy (CloudWatch Alarms only support one Metrics Insights query per alarm). Fixed in `infra/lib/lanes/j-dashboard.ts` (mechanical infra-only fix, J's own file) by reducing to a single-query raw error-count alarm; documented inline. Redeployed clean.
   - **M:** `GET /v1/public/insights` 500'd against real data (`Cannot convert undefined or null to object`) because M's handler assumed `STATS#IMPACT`/`ALL`'s `document` field was a full `ImpactStatsDocument` with a nested `byMonth`, but S's `compute-stats` actually writes just the overall `ImpactStats` there and stores each month as its own separate `STATS#IMPACT`/`<month>` item - a real cross-lane shape mismatch invisible to either lane's own fixture-based unit tests. Fixed in `services/insights/src/handlers/public-insights.ts` (M's own file) by querying `PK = STATS#IMPACT` and assembling the document from the `ALL` item plus every month item; test mock updated to match. Redeployed clean.
4. Ran `LaneSStack-int`'s already-deployed `compute-stats` Lambda for real against `int`'s FlaggedBatches table, which already held 1032 real ingested rows across 11 months (from A1/A2's earlier ingestion runs) - this was the real backfill M and S's own Handoffs were both waiting on. `/v1/public/stats`, `/v1/public/insights`, and `/v1/public/metrics` all re-verified live afterward; M's `byCategory` (1023 NSQ + 9 SPURIOUS = 1032) exactly matches S's `totalFlaggedBatches`.
5. Verified N's `POST /v1/pharmacy/checks` end-to-end against real `int` data: a real 200-row CSV (3 real seeded batch numbers + 197 filler rows) via direct Lambda invocation, warm executions at 3.0-3.4s (well under the 5s bar), correct FLAGGED/NO_ALERT_FOUND split (manufacturer mismatch correctly downgraded 2 of the 3 seeded batches - matching logic, not a bug).
6. Verified L's `POST /v1/reports` end-to-end against real `int` data: a real batch identity, 201 returned with real PvPI routes, and the stored DynamoDB item confirmed to hold no personal data (no `userId`, no product name, no manufacturer) - matches the privacy design.
7. **No live Cognito user was available this session** (the `e2e-test@asli.internal` user from the prior session exists but its password isn't known to this session and wasn't stored anywhere retrievable, and resetting it was outside this session's permission scope) - N's and L's authenticated handlers were invoked directly with a synthetic `requestContext.authorizer.jwt.claims.sub`, the same technique the prior X session used to verify H's authz handlers directly against real fixture data. `tests/e2e` itself was not re-run this session (nothing about the Scan/Core gates changed).
8. `pnpm -r lint && pnpm -r test && pnpm -r build` green repo-wide after every change in this session.
**Contract change requests:** none new (the J alarm and M query-shape fixes were infra/wiring-only, not `packages/contracts` changes).
**Remaining for a later session:** Z1/Z2 (deliberately not started per the human's instruction - "don't start anything from Z1/Z2 yet"); a human should compare S's now-real numbers against docs/PRODUCT.md's Evidence table and decide whether to update it; K still needs real QR photos; N's/L's invoice-photo and any future Bedrock-dependent path remain blocked account-wide; a real Cognito sign-in re-verification of N/L (vs. this session's synthetic-authorizer invocation) would be worth doing once a live test-user password is available again.

## Merge log
| Time | Lane | Commit | e2e | Notes |
|---|---|---|---|---|
| 2026-09-18 00:21 | T02 | `99ef790` merge(T02) | n/a | contracts-v1 + shared-stack, prerequisite for B and all Wave 1 lanes. |
| 2026-09-18 00:22 | B | `792c59e` merge(B) | n/a | Matching library, human-reviewed. |
| 2026-09-18 00:49 | A1 | `98d0663` merge(A1) | n/a | CDSCO endpoint client and parser. |
| 2026-09-18 12:12 | A2 | `ec277c6` merge(A2) | n/a | Ingestion state machine, backfill, demo replay, build-reference. |
| 2026-09-18 12:15 | C | `d1e6a20` merge(C) | n/a | Upload, scan, check and alert-detail APIs. |
| 2026-09-18 17:10 | G1 | `92dabd6` merge(G1) | n/a | Push/email subscription API and senders. |
| 2026-09-18 17:10 | G2 | `f96ea7e` merge(G2) | n/a | New-alert fan-out stream consumer. |
| 2026-09-18 17:16 | F | `9fa6533` merge(F) | n/a | Cabinet API and retroactive check. |
| 2026-09-18 17:20 | D1+D2 | `54beaf6` merge(D2) | n/a | Web shell + scan/confirm/result screens (D1 and D2 built and merged together, human-approved). |
| 2026-09-18 17:25 | D3 | `290b23e` merge(D3) | n/a | Cabinet, medicine detail, members, and notification deep link screens. |
| 2026-09-18 17:28 | T01 | `0b33961` merge(T01) | n/a | AWS spikes - endpoint OK, PDF listing OK, Bedrock/Translate/Textract/AVP blocked by account restriction. |
| 2026-09-18 17:29 | H | `54e4d8d` merge(H) | n/a | Caregiver permissions (Cedar/AVP), invites and members API. |
| 2026-09-18 17:32 | (main) | `ab6bd95` fix(main) | n/a | Repaired `pnpm-lock.yaml` after the multi-lane merge burst above. |
| 2026-09-18 (later) | E | `d304fd3` feat(E) | n/a | Accuracy harness - label CLI, runner, scoring, seeded tier probes. Committed direct to main. |
| 2026-09-18 (later) | I | `a861075` feat(I) | n/a | Content package, guidance API, translations, read-aloud scripts. Committed direct to main. |
| 2026-09-18 (later) | S | `182bbe2` feat(S) | n/a | Stats job and public stats API. Committed direct to main. |
| 2026-09-18 (later) | J | `da82557` feat(J) | n/a | Cost/accuracy dashboard, CloudWatch dashboard/alarms/budget. Committed direct to main. |
| 2026-09-18 (later) | M | `19acf86` feat(M) | n/a | Public insights page. Committed direct to main. |
| 2026-09-18 (later) | L | `ae8968e` feat(L) | n/a | Report-a-problem routed to PvPI, deployed to `dev-l`. Committed direct to main. |
| 2026-09-18 (later) | N | `0ede6bf` feat(N) | n/a | Pharmacy bulk-check mode. Committed direct to main. |
| 2026-09-18 (later) | K | `7e2dc9e` feat(K) | n/a | QR decoding (in-browser jsQR + GS1/URL/key:value parsing). Committed direct to main. |
| 2026-09-19 | X | `850c6fa` feat(X) | Wave 2 int deploy | Deployed J/L/M/N to `int`; destroyed `dev-l` first; wired L's ReportProblemButton into ResultCard.tsx/MedicineDetailPage.tsx; fixed J's LambdaErrorRateAlarm (Metrics Insights CFN limit) and M's public-insights handler (cross-lane STATS#IMPACT shape mismatch with S); ran S's compute-stats for real (1032 rows/11 months); verified J/L/M/N/S live against real int data. |
| 2026-09-19 | Z1 | (uncommitted this session) | n/a | Hardening pass: audited DLQs/alarms/budget/logs/wording/source-links/CDSCO-outage-resilience/cold-start/git-history (all pass); wrote and ran `scripts/seed-demo.ts` for real (two real Cognito users, real "Mom's medicines" cabinet, disclosed mock-strip medicine live-matched to FLAGGED against the real seeded CDSCO row); purged 2 stale pre-fix DLQ messages (clearing the stream-dlq-depth alarm); redeployed `int` from `main` via `cdk deploy --all` (13/13 stacks, clean). Human explicitly approved the three AWS-mutating actions after reviewing the audit trail. |
