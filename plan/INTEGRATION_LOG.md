# INTEGRATION_LOG.md (maintained by lane X)

## Gates
| Gate | Target (IST) | Status | Time passed | Evidence |
|---|---|---|---|---|
| Stage 0 | Thu 11:00 | ✅ | | Amplify URL **live**: https://main.d2ag2oukltn4mc.amplifyapp.com. T01 verdict **known**: ENDPOINT_OK. **contracts-v1 tagged** on the deployed lane/T02 commit, pushed to origin. |
| Scan | Fri 12:00 | ✅ | 2026-09-18 21:09 IST | `tests/e2e` against `int`: manual check on a real seeded FlaggedBatch row (`GTL 1258` / Amoxicillin, NSQ) returns FLAGGED with a real cdscoonline.gov.in source URL and alertMonth. (Real strip photo → Bedrock vision scan still blocked account-wide, see T01; `POST /v1/scans` code path is fixture-tested only.) |
| Core | Fri 20:00 | ✅ | 2026-09-18 21:09 IST | `tests/e2e` against `int`: add medicine (`AKL1000` / Paracetamol, NSQ) → retroactive check → FLAGGED MATCH within ~2s (well under the 10s bar) → AlertEvent → both G1 PushSenderHandler and EmailSenderHandler confirmed (CloudWatch Logs) processing the event for that cabinet within 20s. Actual push/email *delivery* not verified (no real push subscription; SES sandbox has no verified test recipient) - only the fan-out path, which is what this gate checks. |
| Early submission | Sat 18:00 | ⬜ | | Submission form confirmation |
| Freeze | Sat 22:00 | ⬜ | | e2e green, no P0 bugs |
| Final submission | {{SUBMISSION_DEADLINE}} − 3 h | ⬜ | | |

## Lane status
| Lane | Pri | Status | Merged to main | Deployed to int | Notes |
|---|---|---|---|---|---|
| T00 | P0 | 🟨 | (initial scaffold commits) | | Amplify Hosting live: https://main.d2ag2oukltn4mc.amplifyapp.com. Only remaining: AWS Budgets alert. |
| T01 | P0 | ✅ | 2026-09-18 | | **DONE, merged to main** (`0b33961`). Endpoint verdict: **ENDPOINT_OK**. Bedrock/Translate/Textract/AVP invocation confirmed blocked by an account-level restriction, not IAM/SCP (verified separately: root-user caller bypasses IAM, `describe-organization` confirms no org/SCP exists, `get-foundation-model` shows the model is ACTIVE but `invoke-model` returns `ValidationException: Operation not allowed` across 4 providers). SES sender identity still needs human AWS-console action. See T01 Handoff and 2026-09-18 18:16 IST learning log entry. |
| T02 | P0 | ✅ | 2026-09-18 | | **DONE, merged to main** (`99ef790`). contracts + shared-stack + api-routes + seed scripts, `pnpm -r lint/test/build` green. Deployed to `dev-shared` - 26 SSM params verified, VAPID keys + fixtures seeded. `contracts-v1` tagged. Verified Permissions deferred (`ENABLE_AVP=false`, account-restriction blocker - see T01). |
| A1 | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`98d0663`). CDSCO endpoint client/parser: real T01-fixture-verified parsing (217 NSQ + 4 Spurious rows), `pnpm -r lint/test/build` green (49 new tests). Real `LaneA1Stack-dev-a1` deploy invoked end-to-end against the live CDSCO endpoint, confirmed S3 write. Deployed to `int` 2026-09-18 by X (`dev-a1` left in place, doesn't register any HTTP routes so no conflict). |
| A2 | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`ec277c6`). Ingestion state machine, daily check-months, backfill, demo replay, build-reference. `pnpm -r lint/test/build` green (90 new tests). Real `LaneA2Stack-dev-a2` deploy - all 6 acceptance criteria verified against it, including a genuine idempotency bug (rowHash depended on evolving aliases) found and fixed only by re-running on real infra. Deployed to `int` 2026-09-18 by X; `dev-a2` **destroyed** first (see note below - shared HTTP API route conflict). |
| A3 | P2 | 🟨 | not merged | | **BLOCKED, on branch `lane/A3` only** (`286a006` feat(A3), not merged to main). Textract/table-reconstruct/subflow code-complete, 33 new tests, `cdk synth LaneA3Stack-dev-a3` clean - but blocked on the same account-wide Textract `SubscriptionRequiredException` as Bedrock/Translate (see T01), **not wired into A2's `build.ts` `ChooseSource` PDF branch** (still routes to `PdfNotImplemented`, that edit belongs to A2/X), and only 2 of 3 required fixture PDFs exist (Jan-2025 PDF never captured). Per CLAUDE.md/cutting rules ("merge only if fully working" for P2, and A3 is P2 since the endpoint verdict is OK), recommend leaving unmerged unless Textract access clears - the PDF fallback isn't on the critical path. |
| B | P0 | ✅ | 2026-09-18 | | **DONE, merged to main** (`792c59e`). All 10 required test cases + 100% branch coverage on classifyMatch/decide, property tests, benchmark. Human reviewed classify.ts against the tier table in chat and approved (task's own acceptance criterion). `@asli/matching` is now real, not a stub. |
| C | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`d1e6a20`). Upload/scan/check/alert-detail APIs + `packages/lookup`. `pnpm -r lint/test/build` green (47 new tests in services/scan, 5 in packages/lookup, 194 total repo-wide). Real `LaneCStack-dev-c` deploy; Checks/Alerts/Uploads invoked directly against real seeded data (FLAGGED result, SPURIOUS-first, clean logs). Scans endpoint code-complete + fixture-tested but not live-verified - blocked on the same account-wide Bedrock restriction as A2/N (see T01). Deployed to `int` 2026-09-18 by X; `dev-c` **destroyed** first (route conflict, see note below); `/v1/checks` verified live by `tests/e2e` (Scan gate). |
| D1 | P0 | 🟨 | 2026-09-18 | | Merged to main alongside D2 (`54beaf6`, commit `feat(D1+D2)` - built in one session, human-approved). Theme, shared components, Cognito auth, API client, MSW mocks, i18n shell frame, service worker/push, settings screen. **Remaining:** real Cognito sign-in not yet tested against a deployed User Pool/`int` - needs `VITE_COGNITO_USER_POOL_ID`/`VITE_COGNITO_USER_POOL_CLIENT_ID`/`VITE_API_BASE_URL` set in Amplify Hosting build env and a smoke test. |
| D2 | P0 | 🟨 | 2026-09-18 | | **Merged to main** (`54beaf6`). Method chooser, capture, confirm/manual entry, result card (all docs/UX.md states), bill results, read-aloud, save-to-cabinet. 24 tests passing incl. banned-words grep. **Remaining:** hi/kn result copy (moves to lane I once it ships); not yet run end-to-end against a real `int` deploy or real device; screen recording not captured. |
| D3 | P0 | ✅ | 2026-09-18 | | **DONE, merged to main** (`290b23e`). Cabinet, medicine detail, members, and notification deep-link screens. |
| E | P1 | 🟨 | | | **IN PROGRESS, not yet merged.** Accuracy harness built: label CLI, runner (upload+scan+checks against a real stage via SSM/Cognito), scoring against `@asli/matching`'s own normalize functions, seeded tier-correctness probes off `flagged-batches.json`, JSON+markdown reports, comparison mode. `pnpm -r lint/test/build` green (20 new tests). Blocked on: photos (0/30 strips, 0/10 bills - human task) and a live `int`/`dev-*` deploy with working scans (Bedrock still blocked account-wide per T01) to actually run against. See plan/tasks/E-accuracy-harness.md Handoff. |
| F | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`9fa6533`). Cabinet CRUD handlers + retroactive check stream consumer, local authz stub (H's package still a placeholder at merge time), 34 unit tests, `pnpm -r lint/test/build` green, `cdk synth LaneFStack-dev-f` clean. Deployed to `int` 2026-09-18 by X. **Bug found and fixed by X during integration:** `infra/lib/lanes/f-cabinet.ts` imported the FlaggedBatches table with `Table.fromTableName` (no index info), so `grantReadData` never covered the GSI1 index the retroactive check queries - every retroactive check was silently failing `AccessDeniedException` and medicines stayed `PENDING` forever. Fixed to `fromTableAttributes({ globalIndexes: ['GSI1'] })`, redeployed, confirmed fixed by `tests/e2e` (Core gate). Mechanical IAM-only fix, no business logic touched. |
| G1 | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`92dabd6`). Subscriptions API + push-sender/email-sender Lambdas + wording-enforced templates, 26 unit tests, `pnpm -r lint/test/build` green, `cdk synth` clean. Deployed to `int` 2026-09-18 by X; PushSenderHandler/EmailSenderHandler confirmed processing real AlertEvents via `tests/e2e` (Core gate) - actual push/email delivery not verified (no real push subscription; SES sandbox has no verified test recipient). |
| G2 | P0 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`f96ea7e`). New-alert fan-out: stream consumer on FlaggedBatches (GSI3 candidate lookup, conditional MATCH puts, latestTier bump, `AlertEvent` publish), 60-day backfill notification guard (age-of-`alertMonth`, not execution-tag - `IngestionState.sourceType` can't distinguish backfill from daily). `pnpm -r lint/test/build` green (19 new tests), `cdk synth` clean. Deployed to `int` 2026-09-18 by X. |
| H | P1 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`54e4d8d`). Cedar `stub`/`avp` authz, invites and members API. 56 new tests. Deployed `LaneHStack-dev-h` and invoked all 4 handlers directly against real seeded fixture data in `stub` mode - OWNER invites, VIEWER denied, invite accept single-use, last-owner guard, all confirmed live. `avp` mode unit-tested only (blocked on the same account-wide Verified Permissions restriction as T01/T02). See plan/tasks/H-permissions-cedar.md Handoff. Deployed to `int` 2026-09-18 by X; `dev-h` **destroyed** first (route conflict, see note below). |
| I | P1 | ✅ | 2026-09-18 | int | **DONE, merged to main** (`a861075`). Content package (`render()`, banned-words check), `GET /v1/content/guidance/{guidanceKey}` API, hand-drafted hi/kn translations. **Remaining (non-blocking):** native-speaker review of hi/kn strings unset; real Amazon Translate/Polly runs blocked (`SubscriptionRequiredException`, same account restriction as Bedrock - Polly also has zero `hi-IN`/`kn-IN` voices in `ap-south-1`, so hi/kn read-aloud falls back to browser `speechSynthesis`, not Polly MP3s); D2/G1 still ship their own hardcoded English stand-ins until they swap over (their work, not I's). Deployed to `int` 2026-09-18 by X. |
| S | P1 | 🟨 | 2026-09-18 | int | **Merged to main** (`182bbe2`). `computeImpactStats`, `compute-stats` Lambda (wired to A2's pre-existing `statsJobArn` SSM hook), `GET /v1/public/stats`, stats-report export script. 13 new tests, `cdk synth LaneSStack-dev-s` clean. Deployed to `int` 2026-09-18 by X. **Remaining:** no real backfilled data exists yet to validate against docs/PRODUCT.md's hand-checked Evidence numbers (49/48, 52/52, 70/70). Note: defines its own `ImpactStatsDocument` outside `@asli/contracts` (see contract change request below), separate from J's operational `StatsDocumentSchema`. |
| J | P2 | ⬜ | | | Core gate now ✅ (F/G1/G2 deployed and live-verified on `int`) - unblocked, can start. |
| K | P2 | ⬜ | | | Core gate now ✅ - unblocked, can start. |
| L | P2 | ⬜ | | | Core gate now ✅ - unblocked, can start. |
| M | P2 | ⬜ | | | S is deployed to `int` but has no real backfilled data yet to compute stats from - still effectively blocked until a backfill runs. |
| N | P2 | ⬜ | | | Core gate now ✅ - unblocked, can start. Scans still blocked on the account-wide Bedrock restriction (see T01) so N's own vision-dependent path will hit the same wall C/A2 did. |
| Z1 | P0 | ⬜ | | | Blocked: waiting on Wave 2. |
| Z2 | P0 | ⬜ | | | Blocked: waiting on Z1. |

**Note (2026-09-18):** this table was significantly out of date - it previously showed A1, A2, C, D1, D2, D3, F, G1, G2 as unmerged or not started when `git log --oneline --all | grep merge` shows all of them already merged to `main`. Reconciled against actual git history above; commit hashes included so this doesn't drift again. Still true and worth escalating: **no lane has deployed its backend to the shared `int` stage yet** - all real deploys so far are to per-lane `dev-<id>` stages. The Scan and Core gates below can't be marked ✅ until lane X does an `int` deploy and runs the real end-to-end checks.

**Note (2026-09-18, update 2):** A3, E, I, S all executed. E, I, S committed directly to `main` (`d304fd3`, `a861075`, `182bbe2` - not via a `merge(X)` commit, but same effect). A3 is the odd one out: still sitting on unmerged branch `lane/A3` (`286a006`), blocked on account-wide Textract access and not wired into A2's `build.ts`, and per the P2 "merge only if fully working" cutting rule it should probably stay unmerged unless Textract clears. **The critical path is now entirely on lane X**: every P0/P1 Wave 1 lane is merged or code-complete, but nothing has been deployed to `int`, so the Scan and Core gates are the actual bottleneck, not any more Wave-1 lane work.

**Note (2026-09-18, update 3 - X's `int` deploy session):** Deployed all 9 Wave-1 lane stacks (A1, A2, C, F, G1, G2, H, I, S) to `int`. Three things discovered only by doing a real `int` deploy, none catchable by per-lane `dev-*` testing alone:
1. **Shared HTTP API is one physical API Gateway across every stage** (SSM-imported, not per-stage) - `dev-a2`, `dev-c`, `dev-h` already owned routes the `int` versions of those same lanes needed, causing `ConflictException` on deploy. Fixed by destroying those three now-redundant per-lane sandbox stacks (their acceptance criteria were already met and logged before this session; nothing was lost) - see AWS `cdk destroy` runs this session. `dev-a1` was left alone since A1 registers no HTTP routes.
2. **Shared Cognito client only had `userSrp` auth enabled** (`infra/lib/shared-stack.ts`, T02-owned) - no lane's test tooling (this session's `tests/e2e`, or E's `tools/accuracy` which already assumed `USER_PASSWORD_AUTH` would work) could sign in a test user non-interactively. Fixed by adding `userPassword: true` to the client's `authFlows` (additive, doesn't remove SRP) and redeploying `SharedStack-dev-shared`.
3. **F's retroactive-check Lambda had no IAM permission to query FlaggedBatches' GSI1** (`Table.fromTableName` vs `fromTableAttributes({ globalIndexes })`, see F's row above) - every retroactive check was failing silently, so newly added medicines never left `PENDING`. Fixed, redeployed `LaneFStack-int`, confirmed by e2e.

A test Cognito user (`e2e-test@asli.internal`, in the shared `dev-shared` pool, credentials known to this session and the human, not committed anywhere) was created for `tests/e2e` and future `tools/accuracy` runs. Built `tests/e2e/` (previously didn't exist - `pnpm -r lint/test/build` green, skips itself when `ASLI_TEST_USER_EMAIL`/`ASLI_TEST_USER_PASSWORD` aren't set so `pnpm -r test` stays green without live credentials). Ran it for real against `int`: both the Scan-gate check (manual check on a seeded row → FLAGGED with a real CDSCO source URL) and the Core-gate check (add medicine → retroactive MATCH within 10s → AlertEvent → both push-sender and email-sender confirmed processing it) passed. **Scan and Core gates marked ✅ above with real evidence**, not just "code complete."

**Still remaining for a later X session:** `scripts/seed-demo.ts` and `fixtures/demo/replay-1.json` (X's own deliverables per the task file, not yet started - needed for the demo-replay path and the "two siblings + Mom's cabinet" seed data); real push/email *delivery* (vs. just fan-out) needs a real push subscription and an SES-verified test recipient; `POST /v1/scans` (real photo → Bedrock vision) is still blocked account-wide (see T01) so the Scan gate's "real strip photo" half is unverified - only the manual-check half is proven live.

## Merge log
| Time | Lane | Commit | e2e | Notes |
|---|---|---|---|---|
| 2026-09-18 00:21 | T02 | `99ef790` merge(T02) | n/a | contracts-v1 + shared-stack, prerequisite for B and all Wave 1 lanes. |
| 2026-09-18 00:22 | B | `792c59e` merge(B) | n/a | Matching library, human-reviewed. |
| 2026-09-18 00:49 | A1 | `98d0663` merge(A1) | n/a | CDSCO endpoint client and parser. |
| 2026-09-18 12:12 | A2 | `ec277c6` merge(A2) | n/a | Ingestion state machine, backfill, demo replay, build-reference. |
| 2026-09-18 12:15 | C | `d1e6a20` merge(C) | n/a | Upload, scan, check and alert-detail APIs. |
| 2026-09-18 17:10 | G1 | `92dabd6` merge(G1) | n/a | Push/email subscription API and senders. |
| 2026-09-18 17:10 | G2 | `f96ea7e` merge(G2) | n/a | New-alert fan-out stream consumer. |
| 2026-09-18 17:16 | F | `9fa6533` merge(F) | n/a | Cabinet API and retroactive check. |
| 2026-09-18 17:20 | D1+D2 | `54beaf6` merge(D2) | n/a | Web shell + scan/confirm/result screens (D1 and D2 built and merged together, human-approved). |
| 2026-09-18 17:25 | D3 | `290b23e` merge(D3) | n/a | Cabinet, medicine detail, members, and notification deep link screens. |
| 2026-09-18 17:28 | T01 | `0b33961` merge(T01) | n/a | AWS spikes - endpoint OK, PDF listing OK, Bedrock/Translate/Textract/AVP blocked by account restriction. |
| 2026-09-18 17:29 | H | `54e4d8d` merge(H) | n/a | Caregiver permissions (Cedar/AVP), invites and members API. |
| 2026-09-18 17:32 | (main) | `ab6bd95` fix(main) | n/a | Repaired `pnpm-lock.yaml` after the multi-lane merge burst above. |
| 2026-09-18 (later) | E | `d304fd3` feat(E) | n/a | Accuracy harness - label CLI, runner, scoring, seeded tier probes. Committed direct to main. |
| 2026-09-18 (later) | I | `a861075` feat(I) | n/a | Content package, guidance API, translations, read-aloud scripts. Committed direct to main. |
| 2026-09-18 (later) | S | `182bbe2` feat(S) | n/a | Stats job and public stats API. Committed direct to main. |
