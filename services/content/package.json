{
  "name": "@asli/content-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@asli/content": "workspace:*",
    "@asli/contracts": "workspace:*",
    "@aws-lambda-powertools/logger": "^2.13.1",
    "@aws-lambda-powertools/metrics": "^2.13.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.146",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
