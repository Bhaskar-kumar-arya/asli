# cdsco/

Placeholder. T01 (spikes) has not run yet in this environment, so no real CDSCO
endpoint/PDF responses have been captured. Once T01 finishes its spike and hands
off sample responses, drop them here (e.g. `endpoint-2025-03-nsq.html`,
`pdf-2024-09-nsq.pdf`) and reference their `snapshotKey` shape in
`flagged-batches.json` if it changes.
